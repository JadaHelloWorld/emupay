version: '2'

services:

  emupay:
    image: 'jadajin/emupay:latest'
    ports:
      - "8100:8100"
    restart: always

  emupay_android:
    image: 'jadajin/emupay_andriod:latest'
    ports:
      - "8101:8101"
    restart: always
